# Production environment — used when building the Docker image
# These are baked into the React build at compile time (CRA behaviour)
REACT_APP_BACKEND_URL=http://51.20.236.0:8080
REACT_APP_ML_URL=http://51.20.236.0:5001

